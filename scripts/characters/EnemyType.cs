/// <summary>Hostile types — not colonists. These hunt <see cref="ColonyCharacter"/> (see combat in <see cref="GridSimulator"/>).</summary>
public enum EnemyType
{
	/// <summary>Fast, short range (3×3).</summary>
	Crazy,
	/// <summary>Two targets per step; short range (3×3).</summary>
	Monster
}
